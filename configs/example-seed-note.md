# Seed note (example)

This file is the example payload for configs/seed-manifest.example.yaml.
A real seed would carry a note from the human to their daimon: context,
intentions, what the embodiment is for. Content is curated and approved
during onboarding; never credentials.
